# Product

This is a minimal TypeScript + Vite starter project ("progect"). It serves as a blank-slate web app scaffold — a starting point for building browser-based applications.

The current UI is a default landing page with a click counter, hero image, and links to Vite/TypeScript documentation. There is no backend, routing, or framework — just vanilla TypeScript running in the browser.
